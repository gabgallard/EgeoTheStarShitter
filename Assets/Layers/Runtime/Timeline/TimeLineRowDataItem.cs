namespace ABXY.Layers.Runtime.Timeline
{
    [System.Serializable]
    public class TimeLineRowDataItem
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
